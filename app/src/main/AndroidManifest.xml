<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grimi.cashless">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--android:theme="@style/Theme.AppCompat.NoActionBar">-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckGroupActivity"></activity>
        <activity android:name=".CheckPersonActivity"></activity>
        <activity android:name=".PayActivity"></activity>
        <activity android:name=".ThanksActivity"></activity>
    </application>

</manifest>
